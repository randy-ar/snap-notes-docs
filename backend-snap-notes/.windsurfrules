# Windsurf Rules — Snap Notes Backend
# Semua aturan ini WAJIB dipatuhi oleh Cascade saat mengerjakan project ini.

## 1. Dokumen Referensi Utama

Sebelum menulis atau memodifikasi kode apapun, SELALU jadikan dua dokumen berikut sebagai acuan:

- **PRD** → `docs/PRD.md` — sumber kebenaran untuk fitur, alur, endpoint, dan spesifikasi non-fungsional
- **Class Diagram** → `docs/CLASS_DIAGRAM.md` — sumber kebenaran untuk model data, relasi, struktur folder, dan Prisma schema
- **API Documentation** → `swagger-spec.yaml` — dokumentasi lengkap endpoint API yang sudah siap digunakan (OpenAPI 3.0)

Jika ada konflik antara instruksi user dan dokumen referensi, tanyakan klarifikasi sebelum mengimplementasi.

---

## 2. Arsitektur & Struktur Folder

- Ikuti struktur folder NestJS yang telah didefinisikan di `docs/CLASS_DIAGRAM.md` Section 5
- Setiap fitur memiliki modul sendiri: `auth`, `struk`, `pengeluaran`, `pemasukan`, `kategori`, `notifikasi`, `dashboard`
- Service bersama (Gemini, Storage, Supabase) diletakkan di `src/common/`
- JANGAN membuat file di luar struktur yang sudah ditetapkan tanpa alasan yang jelas

---

## 3. Model Data & Database

- Gunakan **Prisma ORM** untuk semua operasi database — JANGAN query raw SQL langsung
- Nama model, tabel, dan kolom bisnis utama menggunakan **Bahasa Indonesia** sesuai `docs/CLASS_DIAGRAM.md`
- Metadata teknis menggunakan Bahasa Inggris: `id`, `createdAt`, `updatedAt`, `penggunaId`, dll
- Field `@map()` di Prisma menggunakan snake_case untuk nama kolom di database
- Relasi antar model HARUS sesuai dengan ERD di `docs/CLASS_DIAGRAM.md` Section 1
- Schema Prisma final ada di `docs/CLASS_DIAGRAM.md` Section 4 — jadikan ini referensi utama

### Model yang tersedia:
`Pengguna`, `PreferensiNotifikasi`, `Kategori`, `Struk`, `ItemStruk`, `Pengeluaran`, `Pemasukan`

---

## 4. Tech Stack — WAJIB digunakan

| Komponen | Teknologi |
|----------|-----------|
| Framework | NestJS (TypeScript) |
| ORM | Prisma |
| Database | PostgreSQL via Supabase |
| Auth | Supabase Auth (email/password) |
| Storage | Supabase Storage |
| AI Parsing | Google Gemini AI (`@google/genai`) |
| API Docs | `@nestjs/swagger` + `swagger-ui-express` |
| Testing | Jest + `@nestjs/testing` |
| Deployment | Vercel |

JANGAN mengganti atau menambah dependency di luar daftar ini tanpa konfirmasi user terlebih dahulu.

---

## 5. Konvensi Kode TypeScript

- Semua kode ditulis dengan **TypeScript strict** — hindari penggunaan `any`
- Gunakan `class-validator` dan `class-transformer` untuk validasi DTO
- Setiap endpoint WAJIB memiliki DTO request dan response yang terpisah
- Gunakan `@Injectable()` untuk semua service
- Error handling menggunakan NestJS built-in exceptions: `NotFoundException`, `ForbiddenException`, `UnauthorizedException`, `ConflictException`, `UnprocessableEntityException`, dll
- Pesan error ditulis dalam **Bahasa Indonesia**

---

## 6. Swagger / API Documentation

- **API Spec** → `swagger-spec.yaml` — dokumentasi OpenAPI 3.0 yang sudah lengkap dan siap digunakan sebagai referensi endpoint
- Setiap **Controller** WAJIB didekorasi dengan `@ApiTags('nama-modul')`
- Setiap **endpoint** WAJIB memiliki `@ApiOperation({ summary: '...' })`
- Setiap **endpoint** WAJIB mendokumentasikan semua kemungkinan response dengan `@ApiResponse`
- Endpoint yang memerlukan autentikasi WAJIB didekorasi dengan `@ApiBearerAuth()`
- Setiap **DTO** WAJIB menggunakan `@ApiProperty()` atau `@ApiPropertyOptional()` pada semua field
- Endpoint upload file WAJIB menggunakan `@ApiConsumes('multipart/form-data')`
- Lihat `docs/PRD.md` Section 13 dan `swagger-spec.yaml` untuk konvensi lengkap

### 6.1 Referensi API Endpoint (dari swagger-spec.yaml)

**Endpoint yang sudah terdokumentasi:**
- `POST /struk/scan` — Scan struk dengan OCR dan AI (multipart/form-data)
- `GET /struk` — Ambil daftar struk pengguna (query: bulan, tahun)
- `GET /struk/{id}` — Ambil detail struk berdasarkan ID
- `PATCH /struk/{id}` — Update struk
- `DELETE /struk/{id}` — Hapus struk
- Dan endpoint lainnya di semua modul (auth, pengeluaran, pemasukan, kategori, notifikasi, dashboard)

**Gunakan `swagger-spec.yaml` sebagai single source of truth** untuk:
- Parameter request (query, path, body)
- Response schemas dan status codes
- Authentication requirements
- Content types (application/json, multipart/form-data)

---

## 7. Unit Testing

- Setiap **Service** WAJIB memiliki file `*.service.spec.ts` di folder modul yang sama
- Test HANYA pada layer **Service** — bukan Controller (kecuali diminta)
- Semua dependency eksternal (Prisma, Gemini AI, Supabase Auth, Storage) WAJIB di-mock menggunakan `jest.fn()`
- Setiap test harus mencakup: happy path, `NotFoundException`, `ForbiddenException`, dan skenario error AI/external service
- Target coverage: **≥ 80%** pada layer Service
- Lihat `docs/PRD.md` Section 14 untuk daftar test case per modul

---

## 8. Autentikasi & Keamanan

- Semua endpoint (kecuali `POST /auth/daftar` dan `POST /auth/masuk`) WAJIB dilindungi oleh `SupabaseAuthGuard`
- JWT token diambil dari header `Authorization: Bearer <token>`
- `penggunaId` diambil dari JWT payload — JANGAN menerima `penggunaId` dari request body
- Validasi kepemilikan resource: pastikan resource yang diakses adalah milik pengguna yang sedang login

---

## 9. Endpoint & Fitur

Daftar lengkap endpoint ada di `docs/PRD.md` Section 7. Ringkasan modul:

- **`/auth`** — daftar, masuk, keluar, refresh, profil
- **`/struk`** — scan struk (OCR+AI), CRUD struk
- **`/pengeluaran`** — CRUD pengeluaran (manual & dari struk)
- **`/pemasukan`** — CRUD pemasukan
- **`/kategori`** — daftar preset+custom, tambah/edit/hapus custom
- **`/notifikasi`** — get/simpan/update preferensi notifikasi
- **`/dashboard`** — ringkasan, kalender, tren, per-kategori

JANGAN menambah endpoint di luar daftar ini tanpa konfirmasi user.

---

## 10. Alur Scan Struk (Fitur Utama)

Ikuti alur berikut secara ketat (lihat `docs/PRD.md` Section 8.1):

1. Client mengirim `rawText` (hasil OCR Google ML Kit) + file gambar
2. Server upload gambar ke **Supabase Storage** → dapatkan URL publik
3. Server buat prompt dari `rawText` → kirim ke **Gemini AI**
4. Validasi response Gemini: pastikan JSON valid sebelum disimpan
5. Simpan `Struk` + array `ItemStruk` ke database via Prisma
6. Buat record `Pengeluaran` yang terhubung ke `Struk` tersebut
7. Return data struk + items ke client

---

## 11. Aturan Tambahan

- JANGAN hardcode API key, URL, atau credential — selalu gunakan environment variable
- File `.env` TIDAK boleh di-commit ke git (sudah ada di `.gitignore`)
- Swagger HANYA aktif jika `SWAGGER_ENABLED=true` di environment variable
- Semua response API menggunakan format JSON konsisten dengan `TransformInterceptor`
- Komentar kode TIDAK perlu ditambahkan kecuali diminta user secara eksplisit
- Setelah selesai menjalankan command terminal, **HENTIKAN semua proses terminal yang berjalan** untuk menghindari resource waste dan konflik proses

### 11.1 Terminal & Background Task Cleanup (CRITICAL)

**WAJIB HENTIKAN** semua terminal dan background task setelah setiap prompt selesai:

```bash
# Commands untuk cleanup (gunakan sesuai kebutuhan)
pkill -f "node|npm|nest"    # Kill Node.js/NestJS processes
killall node                 # Kill all node processes
ctrl+c                       # Stop foreground process
```

**Rule ketat:**
- Jika menjalankan `npm run start:dev` → Hentikan dengan `Ctrl+C` atau `killall node` setelah selesai
- Jika menjalankan `npx prisma studio` → Hentikan setelah selesai inspect
- Jika menjalankan test watch mode → Hentikan dengan `Ctrl+C`
- Jika ada background process (daemon, watch) → Kill explicitly
- **TIDAK BOLEH** membiarkan process berjalan setelah prompt selesai

**Consequences:**
- Resource waste (RAM, CPU)
- Port conflicts (3000, 5555, dll)
- Database connection leaks
- File lock issues
- Performance degradation

---

## 12. Role-Based Instructions

Cascade bekerja dengan 3 role spesialis dalam project ini. Role ini aktif berdasarkan konteks file yang sedang di-edit atau direktori tempat file berada.

### 12.1 Backend Developer Role
Aktif saat bekerja di direktori `src/` dan file-file implementasi:
- Fokus pada implementasi endpoint, service, dan controller
- Pastikan semua code mengikuti aturan di Section 1-11
- Gunakan Prisma untuk semua operasi database
- Implementasi error handling yang proper
- Pastikan DTO validation lengkap

### 12.2 Quality Assurance (QA) Role
Aktif saat bekerja di direktori `test/` dan file-file testing:
- Fokus pada penulisan unit test untuk Service layer
- Mock semua external dependency (Prisma, Gemini, Supabase)
- Target coverage ≥ 80% pada Service layer
- Test harus mencakup: happy path, error cases, edge cases
- Pastikan test independen dan bisa run secara parallel

### 12.3 DevOps Role
Aktif saat bekerja di root level, konfigurasi deployment, dan CI/CD:
- Fokus pada konfigurasi Vercel, environment variables
- Setup database migrations (Prisma)
- Konfigurasi CI/CD pipeline
- Pastikan security best practices diikuti
- Monitor dan optimasi deployment

---

## 13. Standarisasi Commit Message

Gunakan Conventional Commits format untuk semua commit. Format: `<type>(<scope>): <subject>`

### 13.1 Type yang digunakan

**fix:** — Perbaikan bug
- Digunakan untuk memperbaiki bug yang ada
- Contoh: `fix(auth): perbaiki validasi email pada endpoint daftar`
- Contoh: `fix(struk): perbaiki error parsing AI saat rawText kosong`

**chore:** — Perubahan maintenance
- Digunakan untuk perubahan yang tidak mempengaruhi fungsionalitas utama
- Update dependency, refactor kode, update konfigurasi
- Contoh: `chore: update package.json ke versi terbaru`
- Contoh: `chore(prisma): refactor query untuk optimasi performance`

**feat:** — Fitur baru
- Digunakan untuk menambahkan fitur baru
- Contoh: `feat(struk): tambahkan endpoint scan struk dengan OCR`
- Contoh: `feat(notifikasi): tambahkan preferensi notifikasi personalisasi`

**deploy:** — Deployment dan konfigurasi
- Digunakan untuk perubahan terkait deployment, environment, CI/CD
- Contoh: `deploy: konfigurasi environment variables untuk Vercel`
- Contoh: `deploy: setup database migration untuk production`

### 13.2 Aturan tambahan
- Subject ditulis dalam **Bahasa Indonesia** dengan huruf kecil
- Subject tidak diakhiri dengan titik
- Gunakan imperative mood (tambahkan, perbaiki, hapus, bukan menambahkan, memperbaiki, menghapus)
- Body commit (jika ada) juga dalam Bahasa Indonesia
- Scope bersifat opsional, gunakan nama modul jika relevan (auth, struk, pengeluaran, dll)
