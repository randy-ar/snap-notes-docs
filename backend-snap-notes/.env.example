# ================================================================
# Snap Notes Backend — Environment Variables
# Copy file ini menjadi .env dan isi dengan nilai yang sesuai
# ================================================================

# ----------------------------------------------------------------
# Database (Supabase PostgreSQL)
# Dapatkan dari: Supabase Dashboard > Project Settings > Database
# ----------------------------------------------------------------
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# ----------------------------------------------------------------
# Supabase
# Dapatkan dari: Supabase Dashboard > Project Settings > API
# ----------------------------------------------------------------
SUPABASE_URL="https://[project-ref].supabase.co"
SUPABASE_ANON_KEY="eyJ..."
SUPABASE_SERVICE_ROLE_KEY="eyJ..."

# ----------------------------------------------------------------
# Supabase Storage
# Nama bucket yang digunakan untuk menyimpan gambar struk
# ----------------------------------------------------------------
SUPABASE_STORAGE_BUCKET="struk-images"

# ----------------------------------------------------------------
# LLM Provider Configuration
# Pilihan: 'gemini' atau 'openrouter'
# ----------------------------------------------------------------
LLM_PROVIDER=gemini

# ----------------------------------------------------------------
# Google Gemini AI (digunakan jika LLM_PROVIDER=gemini)
# Dapatkan dari: https://aistudio.google.com/app/apikey
# ----------------------------------------------------------------
GEMINI_API_KEY="AIza..."
GEMINI_MODEL="gemini-2.5-flash"

# ----------------------------------------------------------------
# OpenRouter (digunakan jika LLM_PROVIDER=openrouter)
# Dapatkan dari: https://openrouter.ai/settings/keys
# ----------------------------------------------------------------
OPENROUTER_API_KEY="sk-or-v1-..."
OPENROUTER_MODEL="google/gemini-1.5-flash"
OPENROUTER_TIMEOUT_MS=28000

# ----------------------------------------------------------------
# JWT (opsional — Supabase mengelola JWT secret secara internal)
# Hanya diperlukan jika verifikasi JWT dilakukan secara manual
# ----------------------------------------------------------------
# JWT_SECRET="your-jwt-secret"

# ----------------------------------------------------------------
# App Config
# ----------------------------------------------------------------
PORT=3000
NODE_ENV="development"

# Aktifkan Swagger UI (true = aktif, false = nonaktif)
# WAJIB false di production
SWAGGER_ENABLED="true"
