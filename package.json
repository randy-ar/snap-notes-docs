{
  "name": "snap-notes-docs-monorepo",
  "private": true,
  "scripts": {
    "dev:landing": "pnpm --filter landing-page dev",
    "build:landing": "pnpm --filter landing-page build",
    "dev:backend": "pnpm --filter backend-snap-notes dev",
    "build:backend": "pnpm --filter backend-snap-notes build"
  }
}
